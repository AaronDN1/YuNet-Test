# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# App/runtime artifacts
smoke_result.txt
probe_result.txt
Quarantine/
face_anonymization_log_*.txt

# Model weights: ignore by default. Never commit non-commercial weights
# (e.g. InsightFace SCRFD/RetinaFace det_10g.onnx / buffalo_*).
*.onnx
# Exceptions: permissively licensed (MIT) weights we intentionally ship.
!models/centerface.onnx
!models/face_detection_yunet_2023mar.onnx
